import Image from './opengraph-image';
import { ogSize } from '@/lib/og-generator';

export const alt = 'Admin Console — PolkaPay';
export const size = ogSize;
export const contentType = 'image/png';

export default Image;

